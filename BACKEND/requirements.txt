# ============= FastAPI & Server =============
fastapi>=0.104.0,<0.120.0
uvicorn[standard]>=0.24.0,<0.31.0
python-multipart>=0.0.6,<0.1.0

# ============= LangChain & Agents =============
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-google-genai>=2.0.0,<3.0.0
langchain-community>=0.3.0,<0.4.0  # Required for agent tools

# ============= LangGraph (State Machine) =============
langgraph>=0.2.0,<0.3.0

# ============= Google Gemini API =============
google-generativeai>=0.8.0,<1.0.0

# ============= Vector Database & Embeddings =============
chromadb>=0.4.0,<0.6.0
sentence-transformers>=2.2.0,<3.0.0
torch>=2.0.0  # Required for sentence-transformers
transformers>=4.30.0  # Required for sentence-transformers

# ============= Data Processing =============
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
openpyxl>=3.1.0,<4.0.0  # Excel file support (.xlsx)
xlrd>=2.0.1,<3.0.0      # Old Excel format support (.xls)

# ============= Database =============
# sqlite3 is built-in with Python, no separate install needed

# ============= Data Validation =============
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# ============= Utilities =============
python-dotenv>=1.0.0,<2.0.0
typing-extensions>=4.8.0,<5.0.0

# ============= HTTP & Networking =============
httpx>=0.25.0,<0.28.0  # Required by LangChain
requests>=2.31.0,<3.0.0

# ============= Logging =============
colorlog>=6.7.0,<7.0.0

# ============= Async Support =============
aiofiles>=23.0.0,<24.0.0

# ============= Type Hints Support =============
annotated-types>=0.6.0  # Required by pydantic v2
